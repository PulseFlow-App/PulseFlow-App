# Copy to .env or .env.local for local development.
# Vite bakes these in at build time (VITE_* only).

# API base URL (no trailing slash). Required for auth/sync, referrals, AI insights, points.
# Use port 3002 (not 3000). Start API: cd apps/api && npm run dev
VITE_API_URL=http://localhost:3002

# Firebase (optional). If set, enables "Sign in with Google".
# VITE_FIREBASE_API_KEY=
# VITE_FIREBASE_AUTH_DOMAIN=pulseflow-ca453.firebaseapp.com   # Use the value from Firebase Console (authDomain). Keeps sign-in opening Google; add Firebase Hosting deploy so this domain serves the app.
# VITE_FIREBASE_PROJECT_ID=
# VITE_FIREBASE_STORAGE_BUCKET=
# VITE_FIREBASE_MESSAGING_SENDER_ID=
# VITE_FIREBASE_APP_ID=

# Solana (optional). For wallet connect, Pulse Lab gate, and on-chain rewards.
# VITE_SOLANA_RPC=https://api.devnet.solana.com
# VITE_PULSE_MINT=   # $PULSE token mint (base58). e.g. from pump.fun coin page.
# VITE_REWARD_PROGRAM_ID=   # Deployed reward_vault program ID (for daily_check_in, redeem).
