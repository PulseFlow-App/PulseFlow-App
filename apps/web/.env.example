# Copy to .env or .env.local for local development.
# Vite bakes these in at build time (VITE_* only).

# API base URL (no trailing slash). Required for auth/sync, referrals, AI insights, points.
# Use port 3002 (not 3000). Start API: cd apps/api && npm run dev
VITE_API_URL=http://localhost:3002

# Firebase (optional). If set, enables "Sign in with Google".
# VITE_FIREBASE_API_KEY=
# VITE_FIREBASE_AUTH_DOMAIN=
# VITE_FIREBASE_PROJECT_ID=
# VITE_FIREBASE_STORAGE_BUCKET=
# VITE_FIREBASE_MESSAGING_SENDER_ID=
# VITE_FIREBASE_APP_ID=
