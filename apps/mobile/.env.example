# Copy this file to .env and fill in the values you need.
# Restart the dev server (npx expo start) after changing .env.

# -----------------------------------------------------------------------------
# Magic (email OTP + Solana wallet)
# -----------------------------------------------------------------------------
# If set, sign-in screen offers "Sign in with Magic" (email OTP, no password).
# Magic creates a Solana wallet for the user; usable for premium/$PULSE.
# Get a publishable key at https://dashboard.magic.link
EXPO_PUBLIC_MAGIC_PUBLISHABLE_KEY=

# Optional: OAuth redirect URI for "Sign in with Google" (Magic OAuth).
# Add this exact URI to your Magic dashboard under OAuth redirect URIs.
# Default: pulse://oauth
EXPO_PUBLIC_MAGIC_OAUTH_REDIRECT_URI=

# -----------------------------------------------------------------------------
# Backend API (optional)
# -----------------------------------------------------------------------------
# If set, the app uses your API for:
#   - Auth: POST /auth/sign-in, POST /auth/sign-up (email sign-in)
#   - Body Signals AI: POST /insights/body-signals
#   - User data: GET/POST /users/me/body-logs, etc.
#   - Premium: GET /premium/status?wallet=...
# Example: https://api.pulseflow.site (no trailing slash)
EXPO_PUBLIC_API_URL=

# -----------------------------------------------------------------------------
# Staking (optional)
# -----------------------------------------------------------------------------
# Where users stake $PULSE to unlock Premium. App only does a read-only check.
# If unset, "Go to staking" opens Pump.fun ($PULSE token page). When you have
# a staking page (e.g. stake.pulseflow.site or Streamflow), set this URL.
# Example: https://stake.pulseflow.site or https://streamflow.io/...
EXPO_PUBLIC_STAKING_URL=

# -----------------------------------------------------------------------------
# Google AI (Gemini) - recommended for AI suggestions in Body Signals
# -----------------------------------------------------------------------------
# Get a free API key at https://aistudio.google.com/app/apikey
# The app uses Gemini for insight, explanation, and "What to improve" (max 3).
EXPO_PUBLIC_GOOGLE_AI_API_KEY=

# -----------------------------------------------------------------------------
# OpenAI (optional fallback)
# -----------------------------------------------------------------------------
# Only used if EXPO_PUBLIC_GOOGLE_AI_API_KEY is not set.
# Get a key at https://platform.openai.com/api-keys
EXPO_PUBLIC_OPENAI_API_KEY=
