# Copy to .env in this directory (apps/api) for local development.
# On Vercel, set these in Settings → Environment Variables for the API project.
# Do NOT put VITE_* or Firebase vars here — those go in apps/web.

# -----------------------------------------------------------------------------
# Required for production (persistent data, auth tokens)
# -----------------------------------------------------------------------------
# If DATABASE_URL is missing or wrong, /auth/sync and /referrals/complete return 500.
# Use a Postgres URL (e.g. Supabase connection string). See docs/setup-database.md.
JWT_SECRET=
DATABASE_URL=

# -----------------------------------------------------------------------------
# Optional but recommended
# -----------------------------------------------------------------------------
# Admin API key for GET /admin/users and POST /admin/points (Bearer token).
# ADMIN_API_KEY=your-secret-key

# CORS_ORIGIN=https://your-web-app.vercel.app
# CORS_ORIGINS=https://app1.com,https://app2.com
# (If unset, all origins allowed. Set to your PWA URL so /insights/body-signals and auth work.)

# Recipe-from-fridge: vision AI (Gemini) to detect ingredients and suggest recipes from 3 fridge photos.
# Get a key at https://aistudio.google.com/apikey — then POST /insights/recipes-from-fridge returns recipes.
# GEMINI_API_KEY=your-gemini-api-key

